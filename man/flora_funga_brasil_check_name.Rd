% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/flora_funga_brasil_check_name.R
\name{flora_funga_brasil_check_name}
\alias{flora_funga_brasil_check_name}
\title{Check Species Names Against Flora e Funga do Brazil Database}
\usage{
flora_funga_brasil_check_name(
  searchedName = "Gardnerina angustata",
  flora_funga_brasil = "",
  if_author_fails_try_without_combinations = TRUE,
  silence = TRUE
)
}
\arguments{
\item{searchedName}{Character. The species name to search for in the database.}

\item{flora_funga_brasil}{Data frame. The Flora e Funga do Brazil database to search against.}

\item{if_author_fails_try_without_combinations}{Logical. If \code{TRUE} (default), attempts matching without author combinations
when initial author matching fails.}

\item{silence}{Logical. If \code{TRUE}, suppresses progress messages. Default is \code{TRUE}.}
}
\value{
A data frame with taxonomic information from Flora e Funga do Brazil,
including original search parameters and verification metrics. All column
names are prefixed with 'fb2020_'.
}
\description{
Checks individual species names against the Flora e Funga do Brazil database
for taxonomic validation and resolution. Returns matched taxonomic information
with verification scores and search notes.
}
\details{
\subsection{Matching Process:}{
\enumerate{
\item Standardizes the input scientific name
\item Attempts exact matching with authors
\item Falls back to matching without authors if needed
\item Handles synonyms by returning accepted names
\item Provides verification scores for match quality
}
}

\subsection{Verification Scores:}{
\itemize{
\item \code{verified_author}: Author matching score (0, 50, 100)
\item \code{verified_speciesName}: Species name matching score (0-100)
\item \code{searchNotes}: Detailed notes on match results
}
}
}
\examples{
\donttest{
# Load the database first
flora_funga_brasil <- flora_funga_brasil_get_data(
  path_results = tempdir(),
  update = FALSE,
  load_distribution = TRUE
)

# Check individual names
result <- flora_funga_brasil_check_name(
  searchedName = "Gardnerina angustata",
  flora_funga_brasil = flora_funga_brasil
)

# View results
print(result)
}

}
\seealso{
\code{\link[=flora_funga_brasil_get_data]{flora_funga_brasil_get_data()}} for downloading the database,
\code{\link[=flora_funga_brasil_check_name_batch]{flora_funga_brasil_check_name_batch()}} for batch name checking,
\code{\link[=standardize_scientificName]{standardize_scientificName()}} for name standardization
}
\author{
Pablo Hendrigo Alves de Melo,
Nadia Bystriakova &
Alexandre Monro
}
